// public/js/custom-auth.js

// File ini dapat digunakan untuk interaktivitas tambahan di masa depan.
// Untuk saat ini, animasi dikelola sepenuhnya oleh CSS untuk performa yang lebih baik.
// Contoh: Menambahkan kelas setelah halaman dimuat untuk memicu animasi
document.addEventListener('DOMContentLoaded', () => {
    // Anda bisa menambahkan logika di sini jika diperlukan.
    console.log('Halaman login siap dengan animasi kustom!');
});
